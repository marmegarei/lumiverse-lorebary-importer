{
  "name": "lumiverse-lorebary-importer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/backend.ts --outfile dist/backend.js --target bun && bun build src/frontend.ts --outfile dist/frontend.js --target browser",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.4.0",
    "lumiverse-spindle-types": "0.6.12",
    "typescript": "^5.6.0"
  }
}
